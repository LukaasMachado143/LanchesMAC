@model ItemListViewModel

<div class="title"> 
    <h2>@Model.CategoriaAtual</h2>
</div>

<div class="container">
    <div class="row">
        @foreach(var item in Model.Itens){
            <partial name="_ItemResumo" for="@item"/>
        }
    </div>
</div>